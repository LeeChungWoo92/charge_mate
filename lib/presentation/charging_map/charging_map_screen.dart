import 'package:flutter/material.dart';

class ChargingMapScreen extends StatelessWidget {
  const ChargingMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('충전소찾기'),
    );
  }
}
